# Required only for the initial PostgreSQL-to-SQLite migration.
DB_URL=postgresql://user:password@localhost/dbname
# Set this to a path on the attached persistent volume.
SQLITE_DB_PATH=./data/chess.db
LOG_LEVEL=info